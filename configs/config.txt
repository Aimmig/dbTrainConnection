# Contains all default application settings
[Default]

# Application Language
language = de

# Maptype (see mapBox documentation, use '_' instead of '-')
MapType = satellite_v9

# Output Date Format
dateFormat = dd.MM.yyyy

# Output Time Format
timeFormat = hh:mm

# Default Map Size
defaultSize = 500
mapMin = 200
mapMax = 1200
mapIncrement = 100
showMapDefault = 1

ActiveFilterDefault = false

# Default Time OffSet
defaultOffSet = 5

# Default size of path
PathSize = 2
PathIncrement = 0.1
pathMin = 0.1
pathMax = 4

# Default opacity of path
PathOpacity = 0.85
opacityIncrement = 0.05
opacityMin = 0.1
opacityMax = 1

# Default path color
PathColor = #ff0000
# Default marker color
MarkerColor = #5555BB
# Default size of marker
MarkerSize = s
# Default size of extra marker    
MarkerColorSpecial = #aa339988
# Default size of special marker
MarkerSizeSpecial = l
